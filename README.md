Inserts a row in the postgres db. Pure Java.

Default server: localhost
Default port: 5432
Default name: tutorialJavaPersistencePureJava
Default username:
Default password:

Execution:
mvn clean package -Dusername=${username} -Dpassword=${password} exec:java
